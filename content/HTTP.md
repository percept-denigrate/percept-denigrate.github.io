HTTP (hypertext transfer protocol) is the main protocol used for websites.

It uses a request-response model, where data is plain text.

It is a layer 7 protocol encapsulated  in [[TCP]], and in [[TLS]] in the case of HTTPS.
